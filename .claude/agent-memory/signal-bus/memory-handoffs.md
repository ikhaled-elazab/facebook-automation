# Memory Handoffs — Learnings for Memory-Coordinator

Agents write MEMORY HANDOFF signals here. Memory-coordinator processes them during Pattern F.

**Format:** `- (YYYY-MM-DD, agent=<name>, session=<id>) <learning content>`
**Processing:** BATCH — accumulated until Pattern F, then memory-coordinator consolidates.
**Cleared:** After memory-coordinator processes.

---

<!-- Entries below this line — append new entries -->
- (2026-06-15, agent=database-expert, session=multibranch-design) Multi-branch v2 schema delta designed: new branches table (1:N under accounts; is_default + partial-unique-index default primitive, NOT circular FK), re-keys 8 tables account_id->branch_id (Group A content=rebuild to drop old col; Group B account_state/account_status/seen_comments/dm_sent=structural rebuild since account_id was PK/UNIQUE), drops 6 cols off accounts (SQLite 3.49.2 via better-sqlite3 11.10.0 supports DROP COLUMN), bumps schema_meta.version 1->2. CONSTRAINT: PRAGMA foreign_keys is a no-op inside a tx — FK-off/on toggle must bracket db.transaction(), not sit inside it. action_log gets branch_id via ADD COLUMN (not rebuild) for per-branch caps.
- (2026-06-15, agent=challenger, session=multibranch-design) Migration strategy STRONG-challenged, 2 BLOCKING: (1) FK-check (foreign_key_check) cannot detect zero-row INSERT...SELECT loss — needs COUNT(__new)==COUNT(old) assertion INSIDE tx before each DROP; (2) WAL backup incomplete — wal_checkpoint(TRUNCATE) returns busy=1 and silently leaves -wal if any reader attached, copying only main .db = torn backup; must check checkpoint return + abort on busy>0. Plus verified app-layer breakage: db.js ACCOUNT_UPDATE_COLUMNS (lines 138-149) + loadConfig.js hydrateAccount (lines 41-47) both reference the 6 to-be-dropped columns — must update atomically with migration or worker crashes on first cycle.
- (2026-06-15, agent=deep-planner, session=multibranch-design) 5-phase plan: P1 schema+migration (challenger-gated), P2 db.js accessors+loadConfig hydrateBranch+worker cycle-per-branch (contract test gate), P3 ATOMIC server trio schemas.js+serializers.js+web/src/api/types.ts + branch UI, P4 verification, P5 close. Risk register R1-R7. loadConfig .id meaning-shifts account->branch (core/state.js keys by .id) — carry accountId/accountName alongside.
- (2026-06-15, agent=elite-engineer, session=multibranch-design) Phase 2 build complete on TEMP DB (live db/fb-bot.db untouched, no commits). Files: migrations/v2_branches.js + migrate.js runner wiring; db.js (SCHEMA_VERSION=2, fixed ACCOUNT_UPDATE_COLUMNS, new BRANCH_UPDATE_COLUMNS, branch accessors, countBranchActionsToday); worker/loadConfig.js (hydrateBranch + loadWorkerConfig branches[]); worker/loop.js cycle-per-branch; core/{state,governor,ban-signal}.js re-keyed to branch_id; test/migration-v2.test.js. Round-trip test 4/4 PASS (lossless, idempotent, BLOCKING-1 fault-injection rollback, hydrateBranch contract). MIGRATION ORDERING GOTCHA caught by full-runner integration test (not the unit test): db.getDb() applies v2 schema head whose branch_id indexes fail on v1 tables even with IF NOT EXISTS — so migrate.js must run migrateToV2 on a RAW connection BEFORE db.getDb(). Three-tier cap: branch.dailyActionCap(NULL=inherit) -> accounts.daily_action_cap CEILING(retained) -> settings.global(0=unlimited); default-branch cap seeded NULL to avoid budget-doubling.
- (2026-06-15, agent=typescript-expert, session=multibranch-design) Phase 3 trio re-gate PASS: branch_count emitted on all 4 account responses (list via single grouped query db.countBranchesByAccount, get/create/update via .length); StatusBranchSummary+StatusAccountSummary exact-key match status.js; setDefault returns serializeBranchWithChildren. tsc exit 0, test/branches.test.js 22/22. status contract pinned by deepStrictEqual exact-key assertion at branches.test.js:619-626 (regression tripwire). Phase 3 (multi-branch server+UI) COMPLETE + verified.
- (2026-06-15, agent=deep-reviewer, session=multibranch-design) Phase 3.5 login surface SECURITY GATE = PASS, 0 CRITICAL/0 HIGH. Plaintext password lifetime verified clean: decrypted sync at server/login-control.js:104, filled once at login-flow.js:411, nulled in run() finally at :337 (runs on every exit path: ok/fail/throw/2FA-timeout/abort). toPublic() is a hard allowlist. 17/17 login.test.js pass. LATENT LANDMINE: the 409 one-login-per-account concurrency guard (login-control.js:147-165) is race-safe ONLY because no await sits between the terminal-state check and sessions.set() (better-sqlite3 + decrypt are sync). If a future refactor adds an async DB/decrypt call there -> TOCTOU double-launch hole. 2 MEDIUM + 3 LOW non-blocking hardening: chmod 0600 on session file (world-readable FB cookies); abortAll fire-and-forget vs 10s hard-exit; registry never reaps terminal sessions.
