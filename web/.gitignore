# Frontend build tooling — never deployed to the VPS.
node_modules/

# Vite build output — regenerated by `npm run ui:build`; not tracked.
dist/

# TS incremental build info
node_modules/.tmp/
*.tsbuildinfo
